# EMF Compat: Quark

Makes Quark emotes work correctly with animated EMF player models.

Brings **Quark** emote poses to **Entity Model Features**.

When a resource pack replaces the player model with an animated Entity Model Features variant, Quark's built-in emotes (wave, salute, dance, custom emotes, etc.) are often overwritten. This addon captures only the body parts that the current emote actually moves, then restores them after EMF runs so the emote stays visible while the rest of the body keeps its resource-pack animation.

Tested with **Fresh Animations: Player Extension**.
