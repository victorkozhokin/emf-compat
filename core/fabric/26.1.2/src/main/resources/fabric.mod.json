{
  "schemaVersion": 1,
  "id": "${mod_id}",
  "version": "${version}",
  "name": "${mod_name}",
  "description": "${mod_description}",
  "authors": ["${mod_authors}"],
  "contact": {},
  "license": "${mod_license}",
  "icon": "icon.png",
  "environment": "client",
  "entrypoints": {
    "client": [
      "strm.emfcompat.core.fabric.EMFCompatCoreFabric"
    ],
    "modmenu": [
      "strm.emfcompat.core.fabric.ModMenuIntegration"
    ]
  },
  "mixins": [
    "emf_compat_core.mixins.json"
  ],
  "depends": {
    "fabricloader": ">=${loader_version}",
    "fabric-api": "*",
    "minecraft": ">=${minecraft_version}",
    "entity_model_features": ">=${emf_version}"
  }
}
